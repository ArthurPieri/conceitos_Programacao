// A variable can only be declared once for the scope
let petName = 'Hal'

// If i want to change the value i can use the variable name and equals to the new value
// without the let
petName = 'Spot'

// 2. There are rules related to variable names
// It needs to start with:
// a. Letters
// b. $
// c. _ (underscore)
// invalid variable => let 3 = 2
let result = 3 + 4

// 3. Variables names cannot be reserved keywords

console.log(petName)